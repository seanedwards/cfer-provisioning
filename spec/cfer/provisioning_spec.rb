require 'spec_helper'

describe Cfer::Provisioning do
  it 'has a version number' do
    expect(Cfer::Provisioning::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
